<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <packaging>jar</packaging>

  <artifactId>mclista-bukkit-plugin</artifactId>

  <parent>
    <groupId>pl.mclista</groupId>
    <artifactId>mclista-bukkit</artifactId>
    <version>1.0.1</version>
  </parent>

  <properties>
    <maven.deploy.skip>true</maven.deploy.skip>
    <maven.compiler.source>8</maven.compiler.source>
    <maven.compiler.target>8</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <repositories>
    <!-- Engine repository -->
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>

    <!-- Commands repository -->
    <repository>
      <id>panda-repository</id>
      <url>https://repo.panda-lang.org/releases</url>
    </repository>
  </repositories>

  <dependencies>
    <!-- Engine -->
    <dependency>
      <groupId>io.papermc.paper</groupId>
      <artifactId>paper-api</artifactId>
      <version>1.21.8-R0.1-SNAPSHOT</version>
      <scope>provided</scope>
    </dependency>

    <!-- Core -->
    <dependency>
      <groupId>pl.mclista</groupId>
      <artifactId>mclista-core</artifactId>
      <version>1.0.1</version>
    </dependency>

    <!-- Commands -->
    <dependency>
      <groupId>dev.rollczi</groupId>
      <artifactId>litecommands-bukkit</artifactId>
      <version>3.10.4</version>
    </dependency>

    <!-- API -->
    <dependency>
      <groupId>pl.mclista</groupId>
      <artifactId>mclista-bukkit-api</artifactId>
      <version>1.0.1</version>
    </dependency>

    <!-- Runtime -->
    <dependency>
      <groupId>com.alessiodp.libby</groupId>
      <artifactId>libby-bukkit</artifactId>
      <version>2.0.0-SNAPSHOT</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.2</version>
        <configuration>
          <finalName>mclista-bukkit</finalName>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <relocations>
                <relocation>
                  <pattern>net.kyori</pattern>
                  <shadedPattern>pl.mclista.bukkit.libs.kyori</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>eu.okaeri.configs.</pattern>
                  <shadedPattern>pl.mclista.bukkit.libs.okaeri</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>dev.rollczi.litecommands</pattern>
                  <shadedPattern>pl.mclista.bukkit.libs.litecommands</shadedPattern>
                </relocation>
              </relocations>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>